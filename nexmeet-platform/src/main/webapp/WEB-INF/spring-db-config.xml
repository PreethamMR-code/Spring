<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/task
           http://www.springframework.org/schema/task/spring-task.xsd">


    <!--
        PROPERTY PLACEHOLDER
        Loads src/main/resources/application.properties
        at startup. Every ${key} reference in this file
        is replaced with the value from that file.

        This file is in .gitignore — secrets never reach
        GitHub. On production (Railway/Render/EC2), set
        the same keys as environment variables instead.

        ignore-unresolvable=false means startup FAILS
        with a clear error if any key is missing — better
        than silently connecting with a null password.
    -->
    <context:property-placeholder
            location="classpath:application.properties"
            ignore-unresolvable="false"/>

    <!--
        COMPONENT SCAN FOR SERVICE AND DAO LAYERS
    -->
    <context:component-scan base-package="com.nexmeet.platform.service,
                                      com.nexmeet.platform.service.impl,
                                      com.nexmeet.platform.dao,
                                      com.nexmeet.platform.scheduler"/>

    <!--
        DATABASE CONNECTION POOL (HikariCP)
        Credentials loaded from application.properties.
        Never hardcoded here.
    -->
    <bean id="dataSource"
          class="com.zaxxer.hikari.HikariDataSource"
          destroy-method="close">
        <property name="driverClassName"
                  value="com.mysql.cj.jdbc.Driver"/>
        <property name="jdbcUrl"
                  value="${db.url}"/>
        <property name="username"
                  value="${db.username}"/>
        <property name="password"
                  value="${db.password}"/>
        <property name="minimumIdle"     value="5"/>
        <property name="maximumPoolSize" value="20"/>
        <property name="connectionTimeout" value="30000"/>
        <property name="idleTimeout"     value="600000"/>
        <property name="maxLifetime"     value="1800000"/>
    </bean>

    <!--
        HIBERNATE SESSION FACTORY
    -->
    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan"
                  value="com.nexmeet.platform.entity"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">
                    org.hibernate.dialect.MySQL5InnoDBDialect
                </prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.hbm2ddl.auto">validate</prop>
                <prop key="hibernate.enable_lazy_load_no_trans">true</prop>
                <prop key="javax.persistence.validation.mode">none</prop>
            </props>
        </property>
    </bean>

    <!--
        TRANSACTION MANAGER
    -->
    <bean id="transactionManager"
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>
    <task:annotation-driven/>

    <!--
        MAIL SENDER
        Credentials loaded from application.properties.
        Switch to real SMTP for production by changing
        mail.host, mail.port, mail.username, mail.password
        in application.properties — no code changes needed.
    -->
    <bean id="mailSender"
          class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host"     value="${mail.host}"/>
        <property name="port"     value="${mail.port}"/>
        <property name="username" value="${mail.username}"/>
        <property name="password" value="${mail.password}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.starttls.enable">true</prop>
                <prop key="mail.smtp.connectiontimeout">5000</prop>
                <prop key="mail.smtp.timeout">5000</prop>
                <prop key="mail.smtp.writetimeout">5000</prop>
            </props>
        </property>
    </bean>

</beans>