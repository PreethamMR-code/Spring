<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        ROOT CONTEXT COMPONENT SCAN
        Scans Service and Repository beans into the ROOT application context.
        Spring Security runs in the root context, so it can only see beans
        registered here — NOT beans in the dispatcher servlet (spring-mvc-config.xml).
        UserDetailsServiceImpl MUST be visible here for authentication to work.
    -->
    <context:component-scan base-package="com.nexmeet.platform.service" />
    <context:component-scan base-package="com.nexmeet.platform.repository" />
    <context:component-scan base-package="com.nexmeet.platform.dao" />

</beans>